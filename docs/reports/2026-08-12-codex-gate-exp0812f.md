1. **인과 승격:** 엄격한 의미의 “직접 원인 확정”은 아직 과도하다. 결과는 verify 마스크 해제가 meta-first 제거에 충분한 강력한 개입 증거다. 그러나 학습 seed가 동일하지 않다면 실제 차이는 마스크 하나가 아니며, 단일 베이크라 seed×처치 변동도 분리되지 않았다. 또한 코퍼스는 byte-identical이 아니라 “지정한 wrong_prefix 셀 외 동일”이어야 하므로 diff/hash 검증이 필요하다. redirect 마스크는 유지됐으므로 verify 마스크의 충분성은 지지하지만, 전체 마스킹의 유일성·필요성까지 증명하지는 않는다.

2. **정확도:** CI가 0을 포함한다는 것은 “유의한 차이를 검출하지 못함”이지 비열등성 증명이 아니다. 사전 지정 비열등성 한계가 없으므로 “정확도 무손상/비열등”은 삭제하고, **“추정치 −1.12pp, CI상 최대 약 −2.83pp 손실 가능”**을 병기해야 한다. n8 반복이 문항 내 종속임을 반영한 500문항 단위 CI인지도 명시해야 한다.

3. **권고 CLAIMS 문구:**

> 동일 init·데이터 구성·학습 레시피에서 verify 1,209행의 wrong_prefix 마스크를 해제한 단일 베이크에서, gs0 첫줄 `<|meta|>` 비율이 1.0000에서 0.0000으로 감소하고 `<think>`가 0에서 1.0000으로 증가했다. 이는 redirect 마스크를 유지한 조건에서도 verify 마스킹 해제가 meta-first를 제거하기에 충분한 강한 인과 증거다. 다만 seed 고정 또는 다중-seed 재현 전에는 마스크를 유일한 직접 원인으로 확정하지 않는다. 정확도 변화는 −1.12pp, CI95 [−2.83,+0.57]pp로, 통계적으로 검출된 차이는 없지만 최대 약 2.8pp 손실 가능성을 배제하지 못한다.

**최종판정: PASS-WITH-EDITS.**  
“마스크 인과 확정”, “정확도 비열등/무손상”, “코퍼스 동일”을 각각 위의 제한된 표현으로 수정해야 한다.
