# ICLR style provenance

The **official ICLR 2026 conference style** was successfully fetched on
2026-07-05 from the ICLR Master-Template repository
(https://github.com/ICLR/Master-Template, `iclr2026/` directory).
No substitute preamble was needed.

- `iclr2026_conference.sty` and `iclr2026_conference.bst` — pristine copies
  kept here for provenance.
- Working copies live in `paper/` (next to `main.tex`) so that plain
  `pdflatex main` and the `Makefile` resolve them without TEXINPUTS tricks.

The submission stays anonymized as long as `\iclrfinalcopy` remains commented
out in `main.tex`.
