# 폐기된 탐침 — 기록용
- `steerability_probe.py` — ICC 추정기에 편향(무작위 과제에서도 0.125). 재려던 값이 이미 데이터에 있었음(ICC 0.90+)
- `reach_shift_probe.py` — «첫 수 도달가능성». 동어반복(답 근처 ρ+0.85 → 멀면 −0.03).
  반사실도 없었음. 후속은 `move_space_probe.py` (분포를 보고, 반사실 포함)
