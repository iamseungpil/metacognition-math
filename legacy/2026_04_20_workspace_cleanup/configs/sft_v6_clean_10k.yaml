# Case B mainline: base_sft + V6 clean 10K (redirect+verify+straight)
# Intent: Clean-data restart from base_sft with non-theatrical meta data
# Hypothesis: Clean redirect data (gpt-5.4 full, pre-meta >= 200) enables real switching
# Evidence class: mainline
model_name_or_path: checkpoints/qwen3_base_sft
dataset_path: data/v6_clean_10k_merged.parquet
output_dir: checkpoints/v6_clean_10k_E19
run_name: v6-clean-10k-E19
wandb_project: metacot-math

num_train_epochs: 3
per_device_train_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 2.0e-6
max_length: 4096
save_strategy: epoch
