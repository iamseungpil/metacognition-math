# Meta-CoT v8 environment (verified working on Azure A100 4×GPU, 2026-04-14)
# Install order matters — torch first, then vllm, then verl (with --no-deps).
#
# Bootstrap:
#   python3 -m venv venv && source venv/bin/activate
#   pip install --upgrade pip wheel setuptools
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install vllm==0.7.1
#   pip install --no-deps verl==0.7.1
#   pip install -r requirements.txt
#
# Post-install patches (run scripts/patch_verl_compat.py):
#   1. verl/utils/fsdp_utils.py — add DTensorSpec fallback for torch<2.6
#   2. verl/workers/rollout/vllm_rollout/utils.py — patch get_device_uuid for NvmlCudaPlatform
#   3. verl/trainer/config/ppo_trainer.yaml — bump update_weights_bucket_megabytes: 2048 → 4096

# --- Core RL stack ---
torch==2.5.1
torchvision==0.20.1
vllm==0.7.1
verl==0.7.1

# --- Transformers stack ---
transformers==4.49.0
accelerate==1.13.0
peft==0.19.0
datasets==4.8.4
huggingface_hub==0.36.2

# --- veRL runtime deps ---
hydra-core==1.3.2
tensordict==0.10.0
torchdata==0.11.0
ray==2.54.1
codetiming==1.4.0
dill==0.4.1
pylatexenc==2.10
pybind11==3.0.3
packaging==25.0

# --- Data / logging ---
numpy==1.26.4
pandas==2.3.3
pyarrow==23.0.1
wandb==0.26.0
tensorboard==2.20.0

# --- Optional (bundled with vllm but listed for reproducibility) ---
xformers==0.0.28.post3
