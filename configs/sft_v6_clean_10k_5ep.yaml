# Case B ablation: base_sft + V6 clean 10K × 5 epochs
# Intent: Test if longer training improves switching without overfitting
# Evidence class: mainline ablation
model_name_or_path: checkpoints/qwen3_base_sft
dataset_path: data/v6_clean_10k_merged.parquet
output_dir: checkpoints/v6_clean_10k_E19b_5ep
run_name: v6-clean-10k-E19b-5ep
wandb_project: metacot-math

num_train_epochs: 5
per_device_train_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 1.0e-6
max_length: 4096
save_strategy: epoch
