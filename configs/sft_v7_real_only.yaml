# V7 Real-data-only ablation: redirect(2500) + verify(329) original only
# Intent: Test if template-generated verify hurts meta quality
# Hypothesis: Real-only data produces better qualitative meta than V7 full
# Evidence class: mainline ablation
model_name_or_path: checkpoints/qwen3_base_sft
dataset_path: data/v7_real_only.parquet
output_dir: checkpoints/v7_real_only_E19v2c
run_name: v7-real-only-E19v2c
wandb_project: metacot-math

num_train_epochs: 5
per_device_train_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 2.0e-6
max_length: 4096
save_strategy: epoch
