description: "Stage-0 FULL rollout DUMP (all easy/medium) for confidence-redirect/verify teacher-distill. Roll out the SFT-init student (v8_meta_inside_strict_sft) n=8 on easy/medium problems from the RL meta_mix parquet, grade with rewards._check_correctness, and dump per-problem (rollouts, grades, answers, pass_rate) to a parquet uploaded to HF (data/rv_rollout_full.parquet). The local teacher build (Stage-0b, TRAPI on host) consumes this; this is the only GPU touch for data-gen. Standard 80G4-H100, vLLM (mirrors pg0_yield_pilot). Tokens via env only."

target:
  service: sing
  name: msrresrchbasicvc
  workspace_name: msra-sh-aml-ws

environment:
  image: amlt-sing/acpt-torch2.7.1-py3.10-cuda12.6-ubuntu22.04

code:
  local_dir: $CONFIG_DIR/

jobs:
  - name: rv_rollout_dump
    sku: 80G4-H100
    sla_tier: Standard
    priority: high
    identity: managed
    submit_args:
      max_run_duration_seconds: 604800
      env:
        _AZUREML_SINGULARITY_JOB_UAI: "/subscriptions/c4c534bc-9978-4974-9c87-551f7c5754ef/resourceGroups/msra-sh-aml-rg/providers/Microsoft.ManagedIdentity/userAssignedIdentities/msra-sh-aml-uai"
        HF_TOKEN: ${HF_TOKEN}
        HUGGING_FACE_HUB_TOKEN: ${HF_TOKEN}
        GH_TOKEN: ${GH_TOKEN}
        CODE_TAR_REVISION: "452731603"
        WANDB_API_KEY: ${WANDB_API_KEY}
        WANDB_KEY: ${WANDB_API_KEY}
        HF_HUB_DISABLE_XET: "1"
        VLLM_WORKER_MULTIPROC_METHOD: spawn
        SIMPLERL_DIR: /scratch/conda_envs/simplerl
    command:
      - nvidia-smi
      - |
        bash -c '
        set +e
        set -o pipefail
        set -x
        mkdir -p /scratch/logs /scratch/models /scratch/eval_results
        cd /scratch
        source /opt/conda/etc/profile.d/conda.sh
        conda activate ptca
        # ── inline GPU spinner from t~0 to close the Singularity suspend-on-idle window
        #    (gpu_keeper.py lives inside the not-yet-downloaded tarball; ptca has torch). ──
        nohup python -c "
        import torch, time
        g = torch.cuda.device_count()
        ts = [torch.randn(2048, 2048, device=f'cuda:{i}', dtype=torch.float16) for i in range(g)]
        while True:
            for i in range(g): ts[i] = (ts[i] @ ts[i]) * 0.5
            time.sleep(2)
        " > /scratch/logs/gpu_keeper_inline.log 2>&1 &
        pip install -q "huggingface_hub<1.0" 2>&1 | tail -1

        curl -fsSL -H "Authorization: token $${GH_TOKEN}" -H "Accept: application/octet-stream" -o /tmp/metacognition.tar.gz https://api.github.com/repos/iamseungpil/metacognition-math/releases/assets/$${CODE_TAR_REVISION}
        tar -xzf /tmp/metacognition.tar.gz -C /scratch

        nohup python /scratch/metacognition/scripts/gpu_keeper.py > /scratch/logs/gpu_keeper_early.log 2>&1 &
        bash /scratch/metacognition/scripts/bootstrap_sdc_node.sh
        export PATH="$${SIMPLERL_DIR}/bin:$${PATH}"
        export CONDA_PREFIX="$${SIMPLERL_DIR}"
        PYBIN=$${SIMPLERL_DIR}/bin/python
        pip uninstall -y hf_xet 2>&1 | tail -1 || true
        # env flag + pip uninstall are INSUFFICIENT (memory hf-xet-upload-pitfall):
        # find_spec stays True -> hard-remove the package dir to force classic LFS.
        find $${SIMPLERL_DIR}/lib -maxdepth 3 -name "hf_xet*" -exec rm -rf {} + 2>/dev/null || true
        $${PYBIN} /scratch/metacognition/scripts/pull_parquets.py 2>&1 | tail -3

        # ── stage the SFT init (v8_meta_inside_strict_sft) from the HF dataset ──
        $${PYBIN} -c "
        from huggingface_hub import snapshot_download
        import os, shutil
        snapshot_download(repo_id=\"iamseungpil/metacot\", repo_type=\"dataset\", token=os.environ[\"HF_TOKEN\"],
            allow_patterns=[\"models/v8_meta_inside_strict_sft/checkpoint-254/*\"], local_dir=\"/scratch/models_dl\")
        src=\"/scratch/models_dl/models/v8_meta_inside_strict_sft/checkpoint-254\"; dst=\"/scratch/models/v8_meta_inside_strict_sft\"
        shutil.rmtree(dst, ignore_errors=True); shutil.copytree(src, dst); print(\"SFT init staged\")
        "
        $${PYBIN} -c "
        import json, os; p=\"/scratch/models/v8_meta_inside_strict_sft/tokenizer_config.json\"
        if os.path.exists(p):
            c=json.load(open(p)); c.pop(\"extra_special_tokens\", None); json.dump(c, open(p,\"w\"), indent=2); print(\"tokenizer patched\")
        "

        export PYTHONPATH=/scratch/metacognition
        cd /scratch/metacognition

        # ════════ rollout dump (vLLM) — uploads data/rv_rollout_full.parquet to HF ════════
        $${PYBIN} scripts/rollout_dump.py \
            --model_path /scratch/models/v8_meta_inside_strict_sft \
            --train_parquet /scratch/metacognition/data/verl_train_meta_mix.parquet \
            --n_problems 4171 \
            --rollout_k 8 \
            --tp_size 4 \
            --out_path /scratch/eval_results/rv_rollout_full.parquet \
            --hf_repo iamseungpil/metacot-rv \
            --hf_path data/rv_rollout_full.parquet \
            2>&1 | tee /scratch/logs/rv_rollout_dump.log
        echo "[YAML] rv_rollout_dump DONE"
        sleep 86400
        '
